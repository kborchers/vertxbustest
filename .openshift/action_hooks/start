#!/bin/bash

. "${OPENSHIFT_REPO_DIR}.openshift/config.example"

echo "Starting Vert.x application!"

nohup bash -c "exec 'vertx' 'run' '$EXAMPLE_DIR/${SERVER_FILE}' &> ${OPENSHIFT_DIY_LOG_DIR}vert.x.log" &> /dev/null &
